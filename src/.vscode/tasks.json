{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/IdentityServer.Backend/IdentityServer.Backend.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}